services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: seeto:latest
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: "8080"
    restart: unless-stopped
